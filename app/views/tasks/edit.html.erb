<h1>id: 
<%= @task.id %>のタスク編集ページ</h1>

<%= render 'form', task: @task %>
  
<%= f.label :content, 'メッセージ' %>

  
<%= f.text_field :content %>


  
<%= f.submit '投稿' %>

<% end %>


<%= link_to '一覧に戻る', tasks_path %>